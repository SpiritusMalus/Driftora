# HealthRoutine E2E sync server — runtime + test deps.
# Versions track the LawDocs backend (proven combo) where shared. SQLite via
# aiosqlite is the dev/test driver (Postgres etc. is an OWNER decision, §G).

fastapi==0.128.8
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.4.0
PyNaCl==1.6.2

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

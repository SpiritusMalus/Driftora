# React Native / Expo pin React tightly; transitive react-dom/react-test-renderer
# resolve to a newer patch and trip npm's strict peer check. This is the
# standard Expo workaround.
legacy-peer-deps=true
